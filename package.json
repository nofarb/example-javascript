{
  "scripts": {
    "test": "vitest run"  // Run Vitest tests
  },
  "devDependencies": {
    "vitest": "^0.20.0",  // Ensure Vitest is installed
    "vite": "^2.9.0"      // Vite for bundling (optional)
  }
}
