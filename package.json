{
  "name": "example-javascript",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run --coverage",  // Ensure this runs the tests with coverage enabled
    "build": "vite build"
  },
  "dependencies": {
    "vitest": "1.6.1",  // Use the version that matches your requirements
    "@vitest/coverage-v8": "1.6.1",  // Coverage reporter for Vitest
    "vite": "latest"  // Ensure you are using a compatible version of Vite
  },
  "devDependencies": {
    // Any additional dev dependencies can go here if needed
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": ["lcov", "json", "html"]
  }
}
