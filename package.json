{
  "name": "@codecov/example-javascript",
  "version": "0.0.1",
  "description": "Codecov Example Javascript",
  "scripts": {
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "vitest": "^3.0.0"
  },
  "devDependencies": {
    "vitest-junit-reporter": "^0.5.0"
  }
}
