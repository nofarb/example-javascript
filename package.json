{
  "name": "example-javascript",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "vitest": "^3.2.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1"
  }
}
