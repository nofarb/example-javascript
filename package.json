{
  "name": "example-javascript",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^28.0.0",  // Use the latest Jest version
    "@jest/coverage": "^28.0.0"
  },
  "dependencies": {
    "vite": "^2.9.0"
  }
}
